# QQ 音乐项目范围覆盖账本

上游快照：`L-1124/QQMusicApi@873255f2774361ac97366bd89a14b8ed9d230aae`

QQ 音乐项目范围覆盖 Q001–Q073 及 Q101–Q104，共 77 个媒体业务方法，并额外验收 7 个使这些业务方法能够真实运行的平台基础单元。当前业务方法进度：`pending=0`、`partial=0`、`implemented=20`、`verified=57`，完成度 `77/77 = 100.00%`。基础单元进度：`pending=0`、`partial=0`、`implemented=0`、`verified=7`；当前代码范围没有缺失单元。评论、社交关系和私信等范围外方法仅登记到 [QQ 扩展候选池](extensions/qq.md)，不计入完成率。

本表固定项目范围与实施优先级；扩展候选池保留参考协议索引，但不构成验收分母。每个阶段内部仍先完成更底层、更常用且能直接构成用户链路的能力，低频曲谱、制作人、个性资料等排在最后。

| 顺序 | 阶段 | 上游方法数 | 实施边界 | 当前完成 |
| ---: | --- | ---: | --- | ---: |
| 1 | 最小协议 + 分类搜索 | 4 | 只搭起真实 CGI 请求所需底座，立即接入歌曲优先的分类搜索、建议与热搜，不先建设无法验收的大型框架 | 4/4（Q001 热词、Q002 移动端补全、Q003 独立快速搜索、Q004 十类分类搜索与强类型 selectors 均已真实验收） |
| 2 | 登录 + 多账户 | 8 | 先完成 QQ/微信/移动端二维码、手机验证码、状态、刷新、退出和 `(platform, account)` 持久化；后续个人歌单、VIP 与播放验收不得继续依赖临时凭据 | 8/8（Q043–Q050 全部代码完成；QQ 互联、微信与 QQ 音乐客户端三类二维码均已重新真实创建、扫码并确认，普通手机验证码也已在平台安全验证后完成单次发送、确认和持久化。QQ、移动端及验证码三类服务器凭据均已真实刷新并通过服务重启恢复；微信调用方凭据已真实完成会话检查且未持久化） |
| 3 | 个人歌单 + Uni 导入 | 11 | 创建/删除歌单、添加/删除歌曲、喜欢/取消喜欢、用户创建歌单、喜欢歌曲、收藏歌单及收藏/取消收藏；公开或账户目录完整分页导入 Uni Playlist | 11/11（Q053–Q056 已用普通 QQ 音乐账户完成隔离歌单的创建、加歌、删除歌曲结果校验、删除歌单和无残留复查；当前自建歌单统一使用可写 `qq:dir:<dirId>` 主引用，公开 `tid` 独立保留，删除响应的 GBK JSON 由限定兼容链解析。Q057/Q058 已完成单曲喜欢、可读确认、取消喜欢和原状态恢复的普通账户闭环；Q062 已真实确认当前账户与同一数值 UIN 的 18 个创建目录身份和顺序一致，Q063 已确认当前账户与同一加密 UIN 的 158 首喜欢歌曲均以两页完整同序终止，Q037 账户特殊目录也与该结果一致。Q064–Q066 已完成公开歌单收藏目录 0→1→0 的读取、收藏、取消收藏和无残留闭环。公开读取、账户读取、Uni 持久化与写入身份均已真实验收） |
| 4 | 歌曲详情 + 歌词显示 | 7 | 批量/单曲详情、普通与逐字歌词、翻译、罗马音、助唱标注、多风格翻译、AI 词典和 `song_type`；作为播放和跨平台严格匹配的数据基础 | 7/7（Q013/Q014 歌曲详情、Q024 完整歌词及 Q101–Q104 助唱、多风格翻译、AI 词典存在性和详情均已真实验收） |
| 5 | 播放/下载 + VIP 权益 | 4 | CDN、全部文件规格、VKey/试听/VIP、用户 VIP 状态；同时接统一流、下载、302 和跨平台 resolver | 4/4（Q040 CDN 调度已真实验收；Q041 已同步 48 种规格并接入统一流、下载、302 与跨平台 resolver，普通登录账户真实取得 34 个规格授权，新增三档彩铃和 `lossless` 播放/完整下载/HTTPS 302 均成功；Q060 已用普通账户真实取得三层强类型 VIP 响应，并确认当前账户与同一数值 UIN 用户路径的稳定摘要完全一致；Q042 已完成 MV 单项/批量多清晰度取流并真实验收） |
| 6 | 高频目录页 | 17 | 榜单、歌手页、专辑页、公开歌单页及其完整歌曲分页，随后补 MV 详情/列表/播放；明确排在登录、个人歌单、歌词和播放链之后 | 17/17（Q011/Q012 已真实返回分组榜单目录和任意 offset 歌曲分页，并以 `qq:chart:<topId>` 隔离榜单和普通歌单身份；Q037 的公开歌单及账户 `qq:dir:201` 特殊目录均已由 provider 与统一 HTTP 真实验证，账户详情总数、首屏身份与 Q063 完整目录一致；Q025–Q027 已完成专辑详情、任意偏移歌曲分页及内地/港台/欧美/韩国/日本/其他六区新专辑目录；Q028/Q029 已分离一次性歌手快照与真实索引分页，热门歌手、地区/性别/组合、14 类流派、A-Z/# 及任意 offset 均已强类型映射和真实验收，QQ 固定 80 项物理窗口被安全适配为 1–100 项逻辑页；Q030–Q036 已完成歌手主页、九类主页标签、批量详情描述、相似歌手、歌曲、专辑和歌手 MV 目录，其中歌曲/专辑/相似推荐的固定过取均已做逻辑窗口适配，主页标签保留原生页码与动态百科块，单歌手详情合并主页头部与描述响应；Q038 已保留原生批量 MV 详情、顺序与重复项；Q039 已完整映射 6 个地区、8 种类型和最新/最热排序，并对筛选与 offset 0/1/2 真实验收） |
| 7 | 其余个人音乐库 | 10 | 收藏专辑/MV、账户主页、关注歌手和黑名单；不阻塞个人歌单与 Uni 的首轮可用链路 | 10/10（Q059 已用普通账户真实验证当前账户主页与同一加密 UIN 用户主页，规范身份一致且没有占位凭据。Q061 已真实验证当前账户与同一加密 UIN 用户的关注歌手目录，两端合法空页、总数和顺序一致。Q067 与 Q051/Q052 已完成收藏专辑目录 0→1→0 的当前/指定用户读取、收藏、取消和无残留闭环，并修复真实 `logo` 返回完整 QQ 图床 URL 的兼容；Q068 两条收藏 MV 路径真实返回相同合法空目录且不冒充歌手 ID。Q070–Q072 已完成歌曲、歌手、风格空目录及歌曲 0→1→0 的添加、签名读取、取消和恢复闭环，修正真实歌曲读取 `IdType=0` 与写入类型 1 的方向差异。Q073 完成歌曲黑名单两阶段 Token 清空且所有事务 Token 脱敏，破坏性成功态仍不在普通账户执行。其余仅 Q073 待适合的可恢复环境联合验收） |
| 8 | 综合搜索、推荐与低频展示 | 16 | 先完成带多字段游标的综合搜索，再接首页/歌单/新歌/猜你喜欢/雷达推荐，以及相关内容、版本、标签、制作人、曲谱、收藏人数和音乐基因 | 16/16（Q005 综合搜索六类分桶、直达项、相关词和同会话多字段续页已真实验收；Q006 首页推荐楼层、强类型动态卡片、重复楼层保留和四字段防重复续页已真实验收；Q007 推荐歌单原生 offset/limit、强类型元数据和连续游标页已由 provider 与 统一 HTTP 真实验收；Q008 六类新歌目录、强类型标签及固定快照本地分页已由 provider 逐类真实验收；Q009 五首猜你喜欢快照、账户可选注入及具体理由优先级已由匿名 provider 真实验收；Q010 雷达推荐已把原生 10 首页码安全转换为任意统一窗口，匿名 provider 跨两页真实验收身份、游标和具体理由；Q015 相似歌曲已同时保留直接相似和相同听众分组，数字 ID/MID 两条真实链路通过；Q016 歌曲标签已保留零 ID、空展示字段、多行文本和平台 taxonomy，数字 ID/MID 两条真实链路通过；Q017 相关歌单已保留直接/听众两支，并以真实直接分支修正参考游标缺陷；Q018 相关 MV 已分离数字换批游标与 VID 资源身份，数字 ID/MID 首批和统一 HTTP 连续两页均真实通过；Q019 同曲版本保持数字 ID/MID 请求分支和有序完整歌曲列表，两条真实统一链均返回 10 项；Q020 制作信息保留角色分组、人员身份、关注态和安全动作，数字 ID/MID 真实统一链各返回 10 组；Q021 曲谱详情保留用户/AI/外部三条协议和图片/文件载体，六条真实统一链返回 21/42/4 项；Q022 曲谱存在性以五个独立标志汇总总可用性，数字 ID/MID 真实统一链均为五项全真；Q023 收藏人数接入单曲与原生批量统一端点，数字 ID/MID/重复项的数值和展示文案真实一致；Q069 音乐基因以强类型完整保留个性维度、听歌报告、动态说明与排序，公开用户的 Provider 和统一 HTTP 匿名真实通过） |

上述 8 阶段合计 77 个方法；阶段只决定先后，不构成删减。已经收口的直接搜索之后立即转入登录与多账户，再完成个人歌单/喜欢/收藏和 Uni 导入；歌词剩余扩展、播放下载与 VIP 随后实施。榜单、歌手页、专辑页及公开目录明确后移，不再阻塞登录、个人歌单和实际播放链。综合搜索仍在直接分类搜索稳定后实现，因为它依赖更复杂的多字段游标；移动端二维码 MQTT 在普通 QQ/微信二维码之后实现；曲谱和制作人继续排在最后。

## 平台基础单元

| 编号 | 基础能力 | 状态 | 验收要求 |
| --- | --- | --- | --- |
| QF01 | JSON CGI `module + method + param` 请求与批量响应 | `verified` | Android JSON 批量包络、默认布尔归一化、逐请求布尔保留、顶层/逐子请求码和缺失字段拒绝已完成；跨页搜索与 `needSingingAnnotations` 布尔分支均真实验收。Web JSON CGI 已由富单曲详情真实验收，QF07 又以单个 Web 包中的两个有序子请求真实验证原生批包；77 个公开方法及安全原始扩展所需的普通/批量兼容分支均已覆盖 |
| QF02 | Web/Android 客户端版本与 `comm` 参数 | `verified` | Android 14.9.0.8 档案、精确 UA、设备与会话 `comm` 已真实验收；独立 Chrome 120 Web UA、`ct=24/cv=4747474/platform=yqq.json` 及完整公共参数已由 Q014 数字 ID/MID 分支真实验收 |
| QF03 | GUID、设备身份与 QIMEI | `verified` | QIMEI RSA/AES 请求、GUID/Android ID/IMEI、24 小时刷新、并发单飞、私有原子持久化和重启复用已真实验收。设备与匿名会话按一个 QQ 客户端安装实例持久化并由多账户安全共享，不伪装成账户凭据；每次认证请求才把 QF06 精确选择的账户字段注入独立 `comm` 与 Cookie，同平台别名之间不会串用密钥 |
| QF04 | QQ 请求签名与 JCE/Tars 分支 | `verified` | 固定 `musics.fcg` 的 `zzc` 签名与发送共用同一份序列化 JSON 字节，时间戳和签名只进入固定查询参数；3 组独立实现向量、Q070 三类请求形状和 QF07 匿名 Android 签名单请求均已验收，后者真实返回业务码 0。普通 JSON 方法仍走原端点；当前上游快照没有要求 JCE/Tars 的公开业务方法，未来出现实际业务分支时必须重新降低状态并接入 |
| QF05 | 业务错误、分页、游标与刷新请求 | `verified` | 共享批响应解析先检查顶层码，再按原请求顺序逐项检查子请求码；认证、权限、冲突、限流与未知码映射稳定，只有明确选择的业务方法可取得非零码和缺省空 data 自行解释，不会让批包成功遮蔽子请求失败。固定页码、任意 offset、多字段游标、换批游标和 refresh 支持/拒绝分支已由综合搜索、首页推荐、推荐歌单/新歌/猜你喜欢/雷达、公开目录及相关内容的单元与真实连续页共同验收；空转、倒退、错位、假续页和溢出均有拒绝测试 |
| QF06 | QQ 凭据注入与多账户存储桥接 | `verified` | `qq_credential_v1` 已从共享私有账户仓库按 `(qq, account)` 精确读取，并注入 Android `comm` 与 Cookie；三类二维码及手机验证码确认后都会原子写入精确账户别名，账户资料和 Debug/错误均不泄漏密钥。刷新按微信、QQ、移动端/验证码三种登录类型构造完整参数，只有上游成功且新凭据通过强类型校验后才原子替换旧代际；退出只删除精确账户。三类二维码确认、手机验证码安全验证与登录、调用方凭据会话检查、QQ/移动端/验证码服务器凭据刷新以及服务重启恢复均已真实通过；微信退出删除的既有验收继续覆盖精确清理语义 |
| QF07 | 安全 QQ 原始 API 扩展 | `verified` | 已接统一单请求与最多 20 项原生批请求，调用方只能选择固定 Android/Web 普通或签名档案，模块、方法、参数对象、深度、节点数、序列化大小、批次标签及显式可接受业务码均有界校验。嵌套 Cookie、token、登录密钥、设备身份、目标 URL/域名、代理、请求头和 UA 注入在取账户或访问网络前拒绝；返回中的凭据、Cookie、会话密钥和设备身份递归脱敏，但正常资源 URL 与分页 token 不受影响。Android 可按 `(qq, account)` 精确注入服务器凭据，Web 当前保持匿名档案且拒绝账户混用；匿名 Android 签名单请求、Web 原生批请求和统一 HTTP 输入输出均已真实/离线联合验收 |

QQ 项目范围收口后的调用方凭证桥已接入 `credential_mode=server|client|both`：三类二维码和手机验证码在首请求固定归属模式，`client` 不配置服务器存储也能返回强类型 `qq_credential_v1`，`both` 保存并返回同一序列化代际；专用请求头已覆盖搜索、歌词、歌单、Uni 导入、播放、下载、会话查询、核心账户读写、推荐、评论和权益链路。调用方模式刷新按登录类型生成新代际且只在完整成功后替换，退出会区分客户端丢弃提示与服务器精确别名删除；公开目录可选登录态与底层扩展 API 仍按跨平台契约继续实施。这些桥接能力不计入上述 77 个媒体业务方法或 7 个项目范围基础单元分母。

## 播放与 Uni Playlist 验收

- QQ 歌曲数字 ID、MID、媒体 MID、`songType` 和文件规格分别保存，不能压成一个字符串。
- 统一流响应保留实际音质、VIP/试听边界、链接有效期、CDN 和必要请求头，可用于播放或下载。
- QQ 必须既可作为原始播放平台，也可作为网易云、Uni Playlist 等内容的回退目标；严格匹配仍使用标题、歌手、专辑、时长、ISRC 和版本标签。
- QQ 的公开歌单、账户创建歌单、收藏歌单和喜欢歌曲列表可作为 `platform=qq&type=...&id=...&account?` 的 Uni 导入来源；完整遍历上游分页并保留重复项与来源顺序。

## 平台功能扩展候选

评论、用户社交关系、私信和私信上传等范围外协议已登记到 [QQ 扩展候选池](extensions/qq.md)。这些条目没有实施承诺、分母或完成率；只有出现明确媒体或账户体验需求时才会选入独立扩展任务。
