# QQ 音乐 Basic 覆盖账本

上游快照：`L-1124/QQMusicApi@873255f2774361ac97366bd89a14b8ed9d230aae`

QQ Basic 覆盖全量账本中 Q001–Q073 及 2026-07-25 新增的 Q101–Q104，共 77 个公开业务方法，并额外验收 7 个使这些业务方法能够真实运行的平台基础单元。当前业务方法进度：`pending=12`、`partial=0`、`implemented=31`、`verified=34`，完成度 `65/77 = 84.42%`。基础单元进度：`pending=2`、`partial=3`、`implemented=1`、`verified=1`。上游扩展使旧实现缺失新分支时会降低完成度，不为维持百分比保留过期状态。

逐方法状态以 [`qq.md`](qq.md) 为准；本表固定实施优先级。每个阶段内部仍先完成更底层、更常用且能直接构成用户链路的能力，低频曲谱、制作人、个性资料等排在最后。

| 顺序 | 阶段 | 上游方法数 | 实施边界 | 当前完成 |
| ---: | --- | ---: | --- | ---: |
| 1 | 最小协议 + 分类搜索 | 4 | 只搭起真实 CGI 请求所需底座，立即接入歌曲优先的分类搜索、建议与热搜，不先建设无法验收的大型框架 | 4/4（Q001 热词、Q002 移动端补全、Q003 独立快速搜索、Q004 十类分类搜索与强类型 selectors 均已真实验收） |
| 2 | 登录 + 多账户 | 8 | 先完成 QQ/微信/移动端二维码、手机验证码、状态、刷新、退出和 `(platform, account)` 持久化；后续个人歌单、VIP 与播放验收不得继续依赖临时凭据 | 8/8（Q043–Q050 全部代码完成；三类二维码创建与等待态真实验收，扫码确认、短信成功、已登录状态/刷新/退出仍待账户联合验收） |
| 3 | 个人歌单 + Uni 导入 | 11 | 创建/删除歌单、添加/删除歌曲、喜欢/取消喜欢、用户创建歌单、喜欢歌曲、收藏歌单及收藏/取消收藏；公开或账户目录完整分页导入 Uni Playlist | 11/11（Q053–Q058 已接入普通歌单创建/删除、歌曲增删和喜欢/取消喜欢，歌曲写入均使用真实 `songId+songType` 并保留批量顺序、重复项及添加/删除编码差异；Q062–Q064 已补任意用户创建/收藏目录、当前/任意用户喜欢歌曲和 Uni 多来源导入；Q065/Q066 已接入公开歌单收藏/取消收藏并严格检查失败 ID。公开非空读取、Uni 持久化与写入身份已真实验收，个人账户各写成功态留待联合验收） |
| 4 | 歌曲详情 + 歌词显示 | 7 | 批量/单曲详情、普通与逐字歌词、翻译、罗马音、助唱标注、多风格翻译、AI 词典和 `song_type`；作为播放和跨平台严格匹配的数据基础 | 7/7（Q013/Q014 歌曲详情、Q024 完整歌词及 Q101–Q104 助唱、多风格翻译、AI 词典存在性和详情均已真实验收） |
| 5 | 播放/下载 + VIP 权益 | 4 | CDN、全部文件规格、VKey/试听/VIP、用户 VIP 状态；同时接统一流、下载、302 和跨平台 resolver | 4/4（Q040 CDN 调度已真实验收；Q041 已同步 48 种规格并接入统一流、无损下载、302 与跨平台 resolver，新增彩铃及登录/VIP 成功态待账户验收；Q060 已完成当前账户 VIP 三层强类型响应和精确多账户隔离，待真实账户联合验收；Q042 已完成 MV 单项/批量多清晰度取流并真实验收） |
| 6 | 高频目录页 | 17 | 榜单、歌手页、专辑页、公开歌单页及其完整歌曲分页，随后补 MV 详情/列表/播放；明确排在登录、个人歌单、歌词和播放链之后 | 17/17（Q011/Q012 已真实返回分组榜单目录和任意 offset 歌曲分页，并以 `qq:chart:<topId>` 隔离榜单和普通歌单身份；Q037 公开歌单 provider 与 release HTTP 已真实验证，账户特殊目录待登录验收；Q025–Q027 已完成专辑详情、任意偏移歌曲分页及内地/港台/欧美/韩国/日本/其他六区新专辑目录；Q028/Q029 已分离一次性歌手快照与真实索引分页，热门歌手、地区/性别/组合、14 类流派、A-Z/# 及任意 offset 均已强类型映射和真实验收，QQ 固定 80 项物理窗口被安全适配为 1–100 项逻辑页；Q030–Q036 已完成歌手主页、九类主页标签、批量详情描述、相似歌手、歌曲、专辑和歌手 MV 目录，其中歌曲/专辑/相似推荐的固定过取均已做逻辑窗口适配，主页标签保留原生页码与动态百科块，单歌手详情合并主页头部与描述响应；Q038 已保留原生批量 MV 详情、顺序与重复项；Q039 已完整映射 6 个地区、8 种类型和最新/最热排序，并对筛选与 offset 0/1/2 真实验收） |
| 7 | 其余个人音乐库 | 10 | 收藏专辑/MV、账户主页、关注歌手和黑名单；不阻塞个人歌单与 Uni 的首轮可用链路 | 10/10（Q059 已接任意用户与当前账户主页，当前账户会把 music ID 换回同一凭据的 `encryptUin`，参考占位凭据被拒绝且匿名真实请求已确认业务码 1000。Q061 已接当前/指定用户关注歌手，把目标加密 UIN 与查看者账户分离，并保留直接 offset、关系状态、粉丝数和隐私锁定。Q067/Q068 已保留当前账户与任意用户收藏专辑/MV、精确分页及完整元数据，不会把歌手 ID 冒充 MV ID；收藏专辑匿名公开空目录真实通过。Q070 已接歌曲、歌手、风格三类不喜欢目录，精确保留各自命令、末项游标、分页 Token 和强类型条目，并修正参考分页器在单类别非空时同时索引三个列表的缺陷。Q071/Q072 已接三类批量添加与取消，保留顺序、重复项和普通 CGI 分支，空取消批次不会发网；Q073 完成歌曲黑名单两阶段 Token 清空且所有事务 Token 脱敏。Q051/Q052 已接单项/批量收藏与取消收藏专辑，保留 `v_albumId` 顺序与重复项并严格处理失败 ID。十项真实登录成功态待联合验收） |
| 8 | 综合搜索、推荐与低频展示 | 16 | 先完成带多字段游标的综合搜索，再接首页/歌单/新歌/猜你喜欢/雷达推荐，以及相关内容、版本、标签、制作人、曲谱、收藏人数和音乐基因 | 4/16（Q005 综合搜索六类分桶、直达项、相关词和同会话多字段续页已真实验收；Q006 首页推荐楼层、强类型动态卡片、重复楼层保留和四字段防重复续页已真实验收；Q007 推荐歌单原生 offset/limit、强类型元数据和连续游标页已由 provider 与 release HTTP 真实验收；Q008 六类新歌目录、强类型标签及固定快照本地分页已由 provider 逐类真实验收） |

上述 8 阶段合计 77 个方法；阶段只决定先后，不构成删减。2026-07-25 起，已经收口的直接搜索之后立即转入登录与多账户，再完成个人歌单/喜欢/收藏和 Uni 导入；歌词剩余扩展、播放下载与 VIP 随后实施。榜单、歌手页、专辑页及公开目录明确后移，不再阻塞登录、个人歌单和实际播放链。综合搜索仍在直接分类搜索稳定后实现，因为它依赖更复杂的多字段游标；移动端二维码 MQTT 在普通 QQ/微信二维码之后实现；曲谱和制作人继续排在最后。

## 平台基础单元

| 编号 | 基础能力 | 状态 | 验收要求 |
| --- | --- | --- | --- |
| QF01 | JSON CGI `module + method + param` 请求与批量响应 | `partial` | Android JSON 批量包络、默认布尔归一化、逐请求布尔保留、顶层/逐子请求码和缺失字段拒绝已完成；跨页歌曲搜索与 `needSingingAnnotations` 布尔分支均真实验收。Web JSON CGI 已由富单曲详情真实验收，签名端点已由 QF04 接入；其余批量兼容分支继续随公开方法验收 |
| QF02 | Web/Android 客户端版本与 `comm` 参数 | `verified` | Android 14.9.0.8 档案、精确 UA、设备与会话 `comm` 已真实验收；独立 Chrome 120 Web UA、`ct=24/cv=4747474/platform=yqq.json` 及完整公共参数已由 Q014 数字 ID/MID 分支真实验收 |
| QF03 | GUID、设备身份与 QIMEI | `partial` | QIMEI RSA/AES 请求、GUID/Android ID/IMEI、24 小时刷新、并发单飞、私有原子持久化和重启复用已真实验收；登录后的按账户身份隔离待 QF06 |
| QF04 | QQ 请求签名与 JCE/Tars 分支 | `implemented` | 已为当前公开方法接入固定 `musics.fcg` 的 `zzc` 签名请求，签名与发送共用同一份序列化 JSON 字节，时间戳和签名只进入固定查询参数；3 组独立实现向量及 Q070 三类请求均已离线验收，普通 JSON 方法仍走原端点。当前上游快照没有要求 JCE/Tars 的公开业务方法；真实登录签名成功态待联合验收 |
| QF05 | 业务错误、分页、游标与刷新请求 | `pending` | 非零子请求码稳定映射；页码、游标和 refresh 行为不丢失 |
| QF06 | QQ 凭据注入与多账户存储桥接 | `partial` | `qq_credential_v1` 已从共享私有账户仓库按 `(qq, account)` 精确读取，并注入 Android `comm` 与 Cookie；三类二维码及手机验证码确认后都会原子写入精确账户别名，账户资料和 Debug/错误均不泄漏密钥。真实登录、多账户重启恢复、刷新原子替换与退出删除仍待账户验收及 Q043–Q045 |
| QF07 | 安全 QQ 原始 API 扩展 | `pending` | 只允许固定 QQ 域名/协议，禁止调用方注入 Cookie、token、代理或任意请求头 |

## 播放与 Uni Playlist 验收

- QQ 歌曲数字 ID、MID、媒体 MID、`songType` 和文件规格分别保存，不能压成一个字符串。
- 统一流响应保留实际音质、VIP/试听边界、链接有效期、CDN 和必要请求头，可用于播放或下载。
- QQ 必须既可作为原始播放平台，也可作为网易云、Uni Playlist 等内容的回退目标；严格匹配仍使用标题、歌手、专辑、时长、ISRC 和版本标签。
- QQ 的公开歌单、账户创建歌单、收藏歌单和喜欢歌曲列表可作为 `platform=qq&type=...&id=...&account?` 的 Uni 导入来源；完整遍历上游分页并保留重复项与来源顺序。

## Basic 之外但仍属全量范围

评论 7 项、用户社交关系 3 项、私信 15 项和私信上传 2 项共 27 项延后到 QQ 全量阶段。它们已经登记在全量账本 Q074–Q100，不会因不属于 Basic 而被忽略。

## 更新规则

- 每完成一个公开方法，同一提交更新 [`qq.md`](qq.md) 的逐项状态和本表对应阶段计数。
- 一个方法有多个登录、平台、分页、文件规格或响应分支时，缺任一必需分支最高只能标为 `partial`。
- 每完成 3 个 QQ 上游公开方法，检查一次 `L-1124/QQMusicApi`；每 6–12 小时或用户主动要求时可提前检查。
- 新增普通音乐能力进入 Basic 并重算当前分母；新增纯评论、私信或用户社交能力登记到全量，不为维持百分比而省略。
